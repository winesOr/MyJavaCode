<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>
    <!--<settings>-->
    <!--    &lt;!&ndash;  设置mybatis输出日志 &ndash;&gt;-->
    <!--    <setting name="logImpl" value="STDOUT_LOGGING"/>-->
    <!--</settings>-->

    <!--设置别名:类名就是别名-->
    <typeAliases>
        <!-- name : 实体类所在的包名(不是实体类的包名也可以) -->
        <package name="cn.wineslj.domain"/>
    </typeAliases>

    <!-- sql mapper(SQL映射文件的位置)   -->
    <mappers>
        <!--
            name:xml文件所在包名，一次性把该包下所有xml文件加载啊
            要求：
                1. mapper文件名称要求和dao接口名称一致，包括大小写
                2. mapper文件名称和dao接口必须咋吃统一目录
            -->
        <package name="cn.wineslj.dao"/>
    </mappers>
</configuration>
    